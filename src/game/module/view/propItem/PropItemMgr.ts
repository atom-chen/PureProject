// TypeScript file
class PropItemMgr {
    /**根据配置获取属性数据字符串 */
    // public static getPropString(prop, color1 = 0xffd369, color2 = 0xffffff): egret.ITextElement[] {
    //     let conf = GameConfig.prop[prop.type];
    //     if (!conf) return;
    //     let str = "<(c" + color1 + ")" + conf["attname"] + " : " + ">" + "<(c" + color2 + ")" + prop.value + ">";
    //     return TextFlowUtils.generateTextFlow(str);
    // }

    /**根据属性集计算战力 */
    // public static getPropPower(): number {
    //     return;
    // }
}